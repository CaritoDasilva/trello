// function listCreator(titleList) {
//   let listingTheLists = [];
//   nameList.forEach((list) => {
//     listingTheLists += list;
//   })
// }


function saveNameList(nameList) {
  nameList = [];
  nameList.push(titleList);
  console.log(nameList);

}

function getListOfHomeworks(cardList) {
  cardList = [];
  cardList.push(titleCard);
  console.log(cardList);
}

function cardCreator(listingCards) {
  let listingCards = [];
  cardList.forEach((card) => {
    listingCards += card;
  })
  return listingCards;
  console.log(listingCards);
};